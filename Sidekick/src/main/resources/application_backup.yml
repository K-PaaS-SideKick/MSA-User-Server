spring:
  config:
    import: vault://
  cloud:
    vault:
      uri: http://dev-vault-ui-12c58-26121886-6be70ad3b1e1.kr.lb.naverncp.com:15201 # Vault 서버 주소
      authentication: token
      token: hvs.ILrmXD0iLYz8vvtuVGWYVGcu
      kv:
        enabled: true
        backend: secret
        default-context: mysql
  datasource:
    url: ${vault.datasource.user}
    username: ${vault.datasource.username}
    password: ${vault.datasource.password}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  application:
    name: User-Service
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
server:
  port: 65534
  forward-headers-strategy: framework

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://198.18.0.169:8761/eureka
    enabled: false
  instance:
    hostname: localhost
    prefer-ip-address: true

logging:
  level:
    org.springframework.cloud.vault: DEBUG
    org.springframework.boot: DEBUG
    com.zaxxer.hikari: DEBUG